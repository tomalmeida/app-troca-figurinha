import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-troca-figurinha',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
